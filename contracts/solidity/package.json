{
    "name": "uwu-contracts",
    "version": "1.0.0",
    "description": "uWu P2P Platform Smart Contracts",
    "scripts": {
        "compile": "hardhat compile",
        "test": "hardhat test",
        "deploy:arc": "hardhat run scripts/deploy.js --network arcTestnet",
        "verify": "hardhat verify"
    },
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^4.0.0",
        "@openzeppelin/contracts": "^5.0.0",
        "hardhat": "^2.19.0",
        "typescript": "^5.0.0"
    },
    "dependencies": {
        "dotenv": "^17.2.3"
    }
}
